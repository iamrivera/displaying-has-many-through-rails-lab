<h1>Patients</h2>
<ul>
<% @patients.each do |patient| %> 
        <li><%= link_to "Name: #{patient.name} Number of Appointments: #{patient.appointments.count}", patient_path(patient) %></li>
        <br />
<% end %> 
</ul>
